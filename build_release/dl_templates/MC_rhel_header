<tr>
    <th colspan="4">For automatic update and easiest installation, enable MediaArea softwares repository for RHEL<br>search for "MediaConch" in your package manager<br>or "yum install mediaconch-gui" (for GUI)<br> or "yum install mediaconch-server" (for Server)<br> or "yum install mediaconch" (for CLI)</th>
    <td><a href="{{ path('ma_repos') }}">Activate MediaArea Repository</a></td>
</tr>
<tr>
    <th colspan="4">EPEL distribution packages (Starting from RHEL 7)<br>search for "MediaConch" in your package manager<br>or "yum install mediaconch-gui" (for GUI)<br> or "yum install mediaconch-server" (for Server)<br> or "yum install mediaconch" (for CLI)</th>
    <td><a href="https://fedoraproject.org/wiki/EPEL" target="_blank">How to install CentOS EPEL</a></td>
</tr>
