<tr>
    <th colspan="4">For automatic update and easiest installation, enable MediaArea software repository for Raspbian<br>search for "RAWcooked" in your package manager<br>or "apt-get install rawcooked"</th>
    <td><a href="{{ path('ma_repos') }}">Activate MediaArea Repository</a></td>
</tr>
<tr>
    <th colspan="5">Notes for installing the packages below:<br>"sudo dpkg -i *deb"<br><br>If you get an error message, use:<br>"sudo apt-get -f install"</th>
</tr>
